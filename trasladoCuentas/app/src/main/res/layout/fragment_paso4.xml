<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#F8F9FA">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Paso 4: Actuaciones"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="@color/principal"
                android:textAlignment="center"
                android:layout_marginBottom="12dp"/>

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Servicios que dBANCA activará para completar su traslado."
                android:textSize="15sp"
                android:textColor="@color/secundario"
                android:textAlignment="center"
                android:layout_marginBottom="32dp"/>

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@android:color/white"
                android:padding="16dp"
                android:elevation="2dp">

                <CheckBox
                    android:id="@+id/chkHabilitar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Habilitar nuevas órdenes de transferencia"
                    android:layout_marginBottom="12dp"
                    android:buttonTint="@color/principal"/>

                <CheckBox
                    android:id="@+id/chkAceptar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Aceptar adeudos domiciliados entrantes"
                    android:layout_marginBottom="12dp"
                    android:buttonTint="@color/principal"/>

                <CheckBox
                    android:id="@+id/chkInformar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Informar a emisores de adeudos"
                    android:buttonTint="@color/principal"/>
            </LinearLayout>

        </LinearLayout>
    </ScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnFinalizar"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_marginTop="16dp"
        android:text="FINALIZAR SOLICITUD"
        app:backgroundTint="@color/principal"
        android:textColor="@android:color/white" />

</LinearLayout>